{
  "name": "hotsheet",
  "private": true,
  "scripts": {
    "build": "bun build --minify --no-clear-screen --outdir app/assets app/*/hotsheet.*"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.13"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
