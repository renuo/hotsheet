{
  "name": "hotsheet",
  "private": true,
  "scripts": {
    "build": "bun build:css && bun build:js",
    "build:css": "bun build --minify --outfile app/assets/stylesheets/hotsheet.css app/stylesheets/application.css",
    "build:js": "bun build --minify --outfile app/assets/javascripts/hotsheet.js app/javascripts/application.ts"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.13"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
